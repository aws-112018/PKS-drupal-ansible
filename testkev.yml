---
- name: "test de la solution drupal"
  hosts: all
  gather_facts: True
  tasks:
    - name: "debut du test"
      debug:
        msg: "test en élaboration"
    - name: "get homepage HTTP code"
      uri:
        url: http://127.0.0.1
    - debug:
        var: ansible_virtualization_type
    - debug:
        msg: "coucou" 
    - set_fact:
        ipadd: "{{ ansible_default_ipv4.address }}"
    - debug: 
        msg: "http://{{ ipadd }}"

